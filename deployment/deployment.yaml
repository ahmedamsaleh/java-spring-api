apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: java-spring-api
spec:
  template:
    spec:
      serviceAccountName: default
      containers:
      - name: spring-api
        image: aasaleh/java-spring-api
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "200m"
